# 图片缩略图与缓存

现有 `/uploads/<filename>` 保留原图读取；添加 `?w=160|320|640|960|1600` 时按需生成 WebP（质量 82）。不修改数据库 URL，不覆盖原图，新上传与历史图片使用相同逻辑。GIF 动画、PDF、无法识别的格式保持原始文件。

后台商品、库存、订单、活动列表使用 160 px 缩略图、懒加载、异步解码。已知商品图片域名转换为当前后台的 `/uploads/`，避免继续绕行旧图片服务器。加载失败回退数据库原地址；第三方、带签名参数及附件链接保持原样。

## 缓存与限制

- 优先本地 uploads，缺失时从原 R2 bucket 下载。复用 S3 客户端，连接超时 3 秒，读取超时 8 秒，最多 2 次尝试。
- 缓存位于 `admin-backend/data/image-cache/`，不进入 Git；现有发布流程排除 data，不随发布删除。R2 原图每小时重新读取，浏览器/CDN 响应缓存 1 小时，支持 ETag / 304。
- 同一文件通过跨进程文件锁合并并发回源；临时文件原子替换，避免读取半成品。64 个固定锁文件限制锁文件数量。
- 新增缓存时按修改时间清理，目标 1 GiB；正在处理及最近 60 秒生成的文件保留，因此突发写入时可能短时超出目标。此目录为可再生缓存，可在维护时清理。
- 变体宽度白名单、32 MiB 源文件处理限制、4000 万像素解码限制、路径越界检查；错误响应不缓存。限制只针对此图片读取路径，不修改原上传文件。
- 缩略图首次请求仍可能等待 R2；缓存命中避免回源。若使用 Cloudflare 自定义缓存规则，缓存键必须包含 `w`，不得忽略查询参数或缓存错误响应。

## 验证与发布

```sh
cd admin-backend
python -m pip install -r requirements.txt
python -m unittest -v test_image_delivery
cd ../frontend
node --test imageUrl.test.js
npm run build
```

先发布本仓库后端及前端，再发布商城仓库。现有 Nginx `/uploads/` 反代可直接使用，不需要修改 R2 密钥或 DNS。检查 `/uploads/<真实文件>?w=160` 返回 `image/webp`，重复携带 ETag 返回 304。回滚时保留原图及数据库，缩略图缓存可以保留。

2026-09-18 本地使用现有 594,678 字节商品图验证：160 px 为 1,294 字节，640 px 为 6,564 字节，1600 px 为 23,454 字节。首次本地转换约 31–251 ms，热缓存约 1 ms；这些不包含 R2 或公网延迟，不代表全部商品图的压缩率。

浏览器隔离页面验证：后台及商城实际组件请求指定尺寸；商城离屏图在滚动后加载；模拟 503 后回退原图；切换图片后重置加载状态。测试未写入生产数据。
